import { useState } from "react";
import { ArrowLeft, LockKeyhole, ShieldCheck } from "lucide-react";
import { Link } from "wouter";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { formatNaira } from "@/lib/storeCatalog";
import { useStoreCart } from "@/hooks/useStoreCart";

export default function Checkout() {
  const { user } = useAuth();
  const { items, subtotal } = useStoreCart();
  const [notice, setNotice] = useState("");
  const submit = (event: React.FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    if (!user) { startLogin(); return; }
    setNotice("Your order details are saved for Paystack test checkout. Connect the Paystack test key to open payment securely.");
  };
  return <div className="min-h-screen bg-[#f6f7fb] text-[#14213d]"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><Link href="/cart" className="inline-flex items-center gap-2 text-sm font-bold text-cyan-700"><ArrowLeft size={17}/> Back to cart</Link><p className="text-sm font-bold tracking-[.18em] text-[#0b1736]">CHECKOUT</p><span className="inline-flex items-center gap-2 text-xs font-semibold text-emerald-700"><LockKeyhole size={15}/> Secure checkout</span></div></header><main className="mx-auto max-w-7xl px-5 py-12 lg:px-8 lg:py-20"><div className="mb-10"><p className="eyebrow">Complete your order</p><h1 className="section-title">Buy with confidence.</h1><p className="mt-4 max-w-2xl text-lg leading-8 text-slate-600">Enter your details so Olucha can confirm delivery and prepare your Paystack payment.</p></div><div className="grid gap-8 lg:grid-cols-[1.1fr_.9fr]"><form className="rounded-[2rem] bg-white p-6 shadow-sm sm:p-9" onSubmit={submit}><div className="grid gap-5 sm:grid-cols-2"><label className="field">Full name<input required defaultValue={user?.name || ""} placeholder="Your full name"/></label><label className="field">Email address<input required type="email" defaultValue={user?.email || ""} placeholder="name@example.com"/></label><label className="field">Phone number<input required placeholder="+234 ..."/></label><label className="field">Country<select defaultValue="Nigeria"><option>Nigeria</option><option>Ghana</option><option>United Kingdom</option><option>United States</option></select></label><label className="field sm:col-span-2">Delivery address<textarea required rows={4} placeholder="Street, city, state, and delivery notes"/></label></div><div className="mt-8 rounded-2xl border border-cyan-100 bg-cyan-50 p-4 text-sm text-cyan-900"><div className="flex items-start gap-3"><ShieldCheck className="mt-0.5 shrink-0 text-cyan-700" size={18}/><p><strong>Paystack test mode.</strong> Payment initialization will open after the secure test credentials are connected. No payment success is fabricated.</p></div></div>{notice && <p className="mt-5 rounded-2xl bg-amber-50 p-4 text-sm font-semibold leading-6 text-amber-800">{notice}</p>}<button className="mt-7 w-full rounded-full bg-[#f7b32b] px-5 py-3.5 text-sm font-bold text-[#0b1736]">{user ? "Continue to Paystack" : "Sign in to continue"}</button></form><aside className="h-fit rounded-[2rem] bg-[#0b1736] p-7 text-white"><p className="text-xs font-bold uppercase tracking-[.16em] text-cyan-300">Your order</p><div className="mt-7 space-y-4">{items.map(({ product, quantity }) => <div key={product.slug} className="flex justify-between gap-4 text-sm"><span className="text-white/70">{product.name} × {quantity}</span><span className="font-semibold">{formatNaira(product.price * quantity)}</span></div>)}{!items.length && <p className="text-sm text-white/60">Your cart is empty. <Link href="/shop" className="text-cyan-300 underline">Browse products</Link></p>}</div><div className="my-7 border-t border-white/15 pt-6"><div className="flex items-end justify-between"><span className="font-semibold">Total</span><span className="text-2xl font-bold">{formatNaira(subtotal)}</span></div></div><p className="text-xs leading-5 text-white/50">Delivery charges and final fulfilment details are confirmed before payment completion.</p></aside></div></main></div>;
}
