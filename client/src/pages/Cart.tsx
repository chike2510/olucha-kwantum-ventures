import { ArrowLeft, ArrowRight, Minus, Plus, ShoppingBag, Trash2 } from "lucide-react";
import { Link } from "wouter";
import { useStoreCart } from "@/hooks/useStoreCart";
import { formatNaira } from "@/lib/storeCatalog";

export default function Cart() {
  const { items, update, remove, subtotal } = useStoreCart();
  return <div className="min-h-screen bg-[#f6f7fb] text-[#14213d]"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><Link href="/shop" className="inline-flex items-center gap-2 text-sm font-bold text-cyan-700"><ArrowLeft size={17}/> Continue shopping</Link><p className="text-sm font-bold tracking-[.18em] text-[#0b1736]">YOUR CART</p><ShoppingBag size={20} className="text-[#0b1736]"/></div></header><main className="mx-auto max-w-7xl px-5 py-12 lg:px-8 lg:py-20"><div className="grid gap-8 lg:grid-cols-[1.2fr_.8fr]"><section><p className="eyebrow">Ready when you are</p><h1 className="section-title">Review your cart.</h1><div className="mt-8 space-y-4">{items.map(({ product, quantity }) => <article key={product.slug} className="flex gap-4 rounded-3xl bg-white p-4 shadow-sm sm:p-5"><div className={`hidden h-28 w-28 shrink-0 rounded-2xl bg-gradient-to-br ${product.color} sm:block`}/><div className="min-w-0 flex-1"><p className="text-xs font-bold uppercase tracking-[.14em] text-cyan-700">{product.category}</p><h2 className="mt-1 text-lg font-semibold text-[#0b1736]">{product.name}</h2><p className="mt-1 text-sm text-slate-500">{formatNaira(product.price)} · {product.unit}</p><div className="mt-4 flex items-center justify-between"><div className="flex items-center gap-2 rounded-full border border-slate-200 px-2 py-1"><button aria-label="Decrease quantity" onClick={() => update(product.slug, -1)} className="rounded-full p-1 hover:bg-slate-100"><Minus size={14}/></button><span className="min-w-6 text-center text-sm font-bold">{quantity}</span><button aria-label="Increase quantity" onClick={() => update(product.slug, 1)} className="rounded-full p-1 hover:bg-slate-100"><Plus size={14}/></button></div><button onClick={() => remove(product.slug)} className="inline-flex items-center gap-1 text-xs font-bold text-rose-600"><Trash2 size={14}/> Remove</button></div></div></article>)}{!items.length && <div className="rounded-3xl bg-white p-12 text-center"><ShoppingBag className="mx-auto mb-4 text-slate-300" size={38}/><h2 className="text-xl font-semibold text-[#0b1736]">Your cart is empty</h2><Link href="/shop" className="mt-5 inline-flex rounded-full bg-[#f7b32b] px-5 py-3 text-sm font-bold text-[#0b1736]">Browse products</Link></div>}</div></section><aside className="h-fit rounded-[2rem] bg-[#0b1736] p-7 text-white shadow-xl"><p className="text-xs font-bold uppercase tracking-[.16em] text-cyan-300">Order summary</p><div className="mt-7 flex justify-between text-sm text-white/70"><span>Subtotal</span><span>{formatNaira(subtotal)}</span></div><div className="mt-3 flex justify-between text-sm text-white/70"><span>Delivery</span><span>Calculated at checkout</span></div><div className="my-6 border-t border-white/15 pt-6"><div className="flex justify-between"><span className="font-semibold">Estimated total</span><span className="text-2xl font-bold">{formatNaira(subtotal)}</span></div></div><Link href={items.length ? "/checkout" : "/shop"} className="inline-flex w-full items-center justify-center gap-2 rounded-full bg-[#f7b32b] px-5 py-3.5 text-sm font-bold text-[#0b1736]">{items.length ? "Continue to checkout" : "Browse products"} <ArrowRight size={16}/></Link><p className="mt-4 text-xs leading-5 text-white/50">Payment is prepared for Paystack test mode. Final delivery details are confirmed during checkout.</p></aside></div></main></div>;
}
